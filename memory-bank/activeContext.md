# Active Context

## Current Focus
[2026-03-16] The user clarified the primary evaluation goal: optimize for accurate curve prediction of baseline-relative `total_time` and total/average power across frequencies, not for hitting a single best discrete frequency recommendation. Treat sweet-spot selection as a downstream interpretation only after the curve itself is trustworthy.
[2026-03-15] The next stage is to test predictor generalization across additional feasible hardware topologies instead of further polishing the already-good `TP=2, PP=4, DP=2` fit. Keep the current Pareto-first predictor/correction stack stable, preserve the solved layered clock-reset cleanup, and avoid extra cleanup success logs unless debugging a new regression.
[2026-03-16] The explanatory topology split is now minimally implemented in the predictor: preserve the new `pipeline_exposed_fraction` / `dp_overlapable_fraction` / `tp_sync_fraction` concepts, keep the calibration search unchanged for now, and evaluate the next adjustment through true cross-topology transfer rather than target-only re-fitting.

## Recent Changes
- [2026-03-17] Validated a broader mid/low-band throughput-shape fix locally and remotely: large source-gap low-band regularization is now weighted by source→target topology distance, but only when calibration explicitly carries source topology features (`reference_topology_features_present=True`). Local replay keeps the measured-band curve fit strong (`TP=1, PP=4, DP=4`: `time≈1.25%`, `avg_power_w≈1.05%`; `TP=1, PP=2, DP=8`: `time≈2.26%`, `avg_power_w≈1.66%`) while lifting both zero-shot defaults to `1185 MHz` with neighborhood `[1185, 1192, 1200]`.
- [2026-03-17] Tried a more aggressive full-curve regularizer locally: reduce transfer trust inside large source observation gaps (not only below the observed minimum). This keeps the measured `1252/1260/1267 MHz` `TP=1, PP=4, DP=4` trio fit essentially unchanged (`time≈1.25%`, `avg_power_w≈1.05%`) and pushes `TP=1, PP=2, DP=8` away from the earlier `757 MHz` collapse, but it still drags the `TP=1, PP=4, DP=4` default down to about `930 MHz`, so it is not yet a good broad fix.
- [2026-03-17] Shifted the model-side focus from topology-specific recommendation debugging to a broader full-curve issue: the deep low-frequency tail was being extrapolated too optimistically outside the source topology's observed band. Added a generic source-observed-band low-frequency extrapolation fallback in `analysis/freq_model/model.py`, and updated calibration to record `reference_min_frequency_ratio` / `reference_max_frequency_ratio`.
- [2026-03-17] This broad tail fix materially improves the underconstrained deep-low-frequency branch on `TP=1, PP=2, DP=8`: the regenerated zero-shot default moved from `757 MHz` back up to `900 MHz` (`[900, 907, 915]`) while preserving the already-improved fit on the measured `1177/1185/1192 MHz` trio.
- [2026-03-17] The same broad tail fix does **not** resolve the whole target-curve story for `TP=1, PP=4, DP=4`: its regenerated default is still around `997 MHz`, which means there is a second, broader low-frequency curve-shape bias inside the source-observed band, not only in the deep extrapolated tail.
- [2026-03-17] Synced the local low-TP anchor fix into the live `sd@v100x16-1` working tree and regenerated the `TP=1, PP=2, DP=8` zero-shot transfer artifact as `/home/sd/Megatron-DeepSpeed/.context/canonical-transfer-tp1pp2dp8-20260317-anchorfix/prediction.json`. On the previously measured diagnostic trio (`1177/1185/1192 MHz`), curve error now matches the local replay (`time≈2.07%`, `avg_power_w≈1.68%`, `energy≈0.44%`), but the unsampled low-frequency branch still pulls the energy-first default down to `757 MHz`, so the full tail is not yet trustworthy.
- [2026-03-17] Implemented a low-TP / `TP=1` transfer-anchor adjustment in `analysis/freq_model/model.py`: sub-max throughput frequency elasticity is now damped by `tp_sync_fraction` / `pipeline_exposed_fraction` / `dp_overlapable_fraction`, and low-frequency power keeps a modest topology-aware retention floor. Local offline replay from `.context/transfer-debug-20260316/summary.md` cut `TP=1, PP=4, DP=4` curve error to `time≈1.25%`, `avg_power_w≈1.05%`, `energy≈0.34%`, and `TP=1, PP=2, DP=8` to `time≈2.07%`, `avg_power_w≈1.68%`, `energy≈0.44%`.
- [2026-03-17] Completed the `TP=1, PP=2, DP=8` static validation trio and summarized it under `.context/real-v100-tp1pp2dp8-static-20260316/`. Curve accuracy is clearly insufficient: `total_time` MAPE is about `11.73%`, `avg_power_w` MAPE about `5.74%`, and `total_energy` MAPE about `5.33%`.
- [2026-03-17] The `TP=1, PP=2, DP=8` result strengthens the hypothesis that the current miss is not only a deep-pipeline (`PP=4`) issue. Even after reducing pipeline depth from `PP=4` to `PP=2`, the predictor still made the same directional mistake on runtime ratios and remained too optimistic about power savings.
- [2026-03-16] Started the diagnostic `TP=1, PP=2, DP=8` topology on `sd@v100x16-1` to distinguish deep-pipeline bias from generic `TP=1` bias. The first baseline attempt OOMed at step 5 on the later pipeline-stage ranks, but an allocator-only retry using `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` completed successfully at `1137.3 s / 3935271.5 J / 3460.1 W / 0.416 tokens/J`.
- [2026-03-16] Generated the canonical zero-shot artifact for `TP=1, PP=2, DP=8` under `.context/canonical-transfer-tp1pp2dp8-20260316/`. The current recommended neighborhood is `[1177, 1185, 1192]` with default `1177 MHz`; predicted runtime ratios are about `1.109/1.103/1.097x`, and predicted energy ratios are tightly clustered near `0.732x`.
- [2026-03-16] Launched the first `TP=1, PP=2, DP=8` static validation trio in sequential screen session `tp1pp2dp8_static_seq_20260316`, covering `1177/1185/1192 MHz` with the allocator expansion env enabled because the unfixed baseline had previously OOMed on this topology.
- [2026-03-16] Reinterpreted the completed `TP=1, PP=4, DP=4` transfer under the clarified user goal: this run does **not** yet validate the predictor, because the real target is curve accuracy for `total_time` and power rather than sweet-spot selection. On the tested trio, `total_time` MAPE is about `7.17%` and `avg_power_w` MAPE is about `5.30%`, while low `total_energy` error (`~1.48%`) appears to come from cancellation between those two biases.
- [2026-03-16] Completed the `TP=1, PP=4, DP=4` zero-shot transfer validation on `sd@v100x16-1` and summarized the measured-vs-predicted comparison locally under `.context/real-v100-tp1pp4dp4-static-20260316/`. The predicted neighborhood `[1252, 1260, 1267]` successfully contained the measured best-energy point `1260 MHz`, so the energy neighborhood generalized well.
- [2026-03-16] The `TP=1, PP=4, DP=4` transfer result is a partial success: baseline-relative energy ratios were accurate within about `1.0-1.9%` relative error, but runtime ratios were directionally wrong. The model predicted `1252/1260/1267 MHz` would be `~4.6-5.6%` slower than baseline, while all three were actually `~1.2-2.3%` faster than baseline.
- [2026-03-16] Among the tested `TP=1, PP=4, DP=4` trio, `1260 MHz` is the minimum-energy point (`3067834.2 J`, runtime ratio `0.9781x`), while `1267 MHz` is only marginally faster (`1286.3 s` vs `1287.9 s`) but clearly worse on energy. Treat `1260 MHz` as the best tested discrete point for this topology.
- [2026-03-16] Took over the work in Conductor workspace `yangon`, restored the memory bank, and confirmed that the local `.context/` was empty even though the live remote artifacts remain under `/home/sd/Megatron-DeepSpeed/.context/`; use the remote `.context/` as the current source of truth until artifacts are synced back into this workspace.
- [2026-03-16] Checked the live V100 validation host again and found that `/home/sd/Megatron-DeepSpeed` is running from a dirty working tree on top of git commit `6629a33`, with local modifications/untracked analysis and experiment files. Treat upcoming generalization measurements as tied to this remote working tree state, not to a clean branch tip.
- [2026-03-16] Chose `TP=1, PP=4, DP=4` as the next proxy-distinct generalization target on `sd@v100x16-1`: it preserves a PP-heavy schedule while removing `TP` synchronization, which directly tests whether the explanatory topology split transfers beyond the current `TP=2` cases. Preflight passed under `v100_tp1pp4dp4_baseline50_validate_20260316_20260316_185119_DGX2-1`, the 50-step unfixed baseline then completed at `1316.7 s / 4174627.4 J / 3170.6 W / 0.392 tokens/J`, and the detached static validation sequence in screen session `tp1pp4dp4_static_seq_20260316` has now completed.
- [2026-03-16] Generated the first canonical zero-shot transfer artifact for `TP=1, PP=4, DP=4` under `.context/canonical-transfer-tp1pp4dp4-20260316/` by reusing the corrected `TP=2, PP=4, DP=2` calibration bundle with the new baseline anchor. The current recommended neighborhood is `[1252, 1260, 1267]` with default `1252 MHz`; predicted runtime ratios are `~1.056/1.051/1.046x`, and predicted energy ratios are tightly clustered near `~0.749x` versus baseline.
- [2026-03-16] Launched the first `TP=1, PP=4, DP=4` static validation trio on `sd@v100x16-1` in sequential screen session `tp1pp4dp4_static_seq_20260316`, covering `1252/1260/1267 MHz`. The boot log is `/home/sd/Megatron-DeepSpeed/experiments/_screen_boot/tp1pp4dp4_static_seq_20260316.log`, and the first run is `v100_tp1pp4dp4_pred50_1252_20260316_192152_DGX2-1`.
- [2026-03-16] Validated the current stabilized predictor on `sd@v100x16-1` and preserved canonical artifacts under `.context/canonical-transfer-tp2pp2dp4-20260316/` and `.context/canonical-transfer-tp2pp1dp8-20260316/`. Current quality snapshot: for `TP=2, PP=2, DP=4`, the canonical recommendation is `1072 MHz` with candidate set `[1072, 1080, 1087]`, which still lands in the previously validated `1080 MHz` neighborhood; for `TP=2, PP=1, DP=8`, the canonical recommendation improves to `1005 MHz` with candidate set `[1005, 1012, 1020]`, which is materially better than the old `975 MHz` miss but still below the measured `1050 MHz` winner.
- [2026-03-16] Switched to an explanatory transfer-time rescaling rule that better matches training mechanics: keep the source topology's curve shape, but rescale low-frequency correction amplitudes on the target topology according to exposed pipeline waiting. In practice, lower-PP targets now receive a stronger low-frequency throughput penalty (because frequency matters more when waiting disappears), while low-frequency power correction is only mildly reduced. On `sd@v100x16-1`, this moves `TP=2, PP=2, DP=4` to `1072 MHz` with neighborhood `[1065, 1072, 1080]`, and `TP=2, PP=1, DP=8` to `1005 MHz` with neighborhood `[997, 1005, 1012]`. Artifacts live under `.context/remote-transfer-rescaled-tp2pp2dp4-20260316-r2/` and `.context/remote-transfer-rescaled-tp2pp1dp8-20260316-r2/`.
- [2026-03-16] Applied the next minimal explanatory fix: restore a `1.0` baseline to low-frequency correction intensity while keeping topology ordering (`PP`-exposed > `TP` sync > `DP` overlapable). On `sd@v100x16-1`, this improves the post-split transfer recheck from `1035 -> 1057 MHz` for `TP=2, PP=2, DP=4` and from `907 -> 960 MHz` for `TP=2, PP=1, DP=8`. New artifacts are under `.context/remote-recheck-tp2pp2dp4-20260316-v100-r2/` and `.context/remote-recheck-tp2pp1dp8-20260316-v100-r2/`.
- [2026-03-16] Executed the first post-split cross-topology transfer recheck on the intended V100 validation host `sd@v100x16-1` using synced baseline artifacts and the updated predictor code. The new explanatory split by itself does not yet improve transfer: `TP=2, PP=2, DP=4` moved from `1080 MHz` down to `1035 MHz`, and `TP=2, PP=1, DP=8` moved from `975 MHz` down to `907 MHz`. Canonical artifacts are staged under `.context/remote-recheck-tp2pp2dp4-20260316-v100/` and `.context/remote-recheck-tp2pp1dp8-20260316-v100/`.
- [2026-03-16] The user clarified that active predictor validation should continue on `sd@v100x16-1` for now; other nodes should wait until the prediction layer is stable.
- [2026-03-16] Preflight on `user@sd-1` still established that this host is `4 x RTX 4080 SUPER` with repo path `/home/user/Megatron-DeepSpeed`; keep it out of the main validation loop until we intentionally redesign a 4-GPU topology set.

- [2026-03-16] Implemented the first explanatory topology split in `analysis/freq_model/features.py` and `analysis/freq_model/model.py`: low-frequency correction is now gated mostly by pipeline-exposed cost, while communication frequency sensitivity uses an exposed-communication proxy instead of raw aggregate communication share.
- [2026-03-16] Added focused unit coverage for the new concepts in `tests/unit_tests/test_freq_model.py` and validated the patch with `python -m py_compile analysis/freq_model/features.py analysis/freq_model/model.py tests/unit_tests/test_freq_model.py` plus `python -m pytest --noconftest -q tests/unit_tests/test_freq_model.py` (`15 passed`).
- [2026-03-16] A quick local target-only re-fit check on `TP=2, PP=2, DP=4` and `TP=2, PP=1, DP=8` is not a reliable transfer verdict: with the current sparse target-only samples, calibration collapses back to zero residual corrections, so the meaningful next validation remains the real cross-topology transfer workflow rather than interpreting those re-fit default points.

- [2026-03-15] Wrote an explanatory adjustment design that grounds future topology fixes in distributed-training mechanics rather than extra fit coefficients. The key long-term concept is to separate `pipeline_exposed_cost`, `dp_overlapable_cost`, and `tp_sync_cost`, and let low-frequency correction track exposed waiting instead of generic topology complexity. See `docs/plans/2026-03-15-topology-explanatory-adjustment-design.md`.
- [2026-03-15] Completed the detached `TP=2, PP=1, DP=8` 50-step transfer validation on `sd@v100x16-1` for `967/975/982/1050 MHz`. This topology is a clear miss for the current transferred correction layer: the predicted default `975 MHz` underestimates the no-bubble regime, while the higher `1050 MHz` control is both the minimum-energy and fastest tested point.
- [2026-03-15] Completed the `TP=2, PP=1, DP=8` 50-step unfixed baseline on `sd@v100x16-1` with Zeus summary `851.9 s / 4166.4 W / 3549352.3 J / 0.462 tokens/J`; the first transferred prediction for this topology now recommends the `967/975/982 MHz` neighborhood with default `975 MHz`.
- [2026-03-15] Launched detached remote 50-step static validation for `TP=2, PP=1, DP=8` in session `power_tp2pp1dp8_pred50_20260315_211016`, covering `967/975/982 MHz` plus a `1050 MHz` control.
- [2026-03-15] Generated the first `TP=2, PP=1, DP=8` transfer prediction from the corrected cross-topology calibration: default `975 MHz`, neighborhood `[967, 975, 982]`, with an additional `1050 MHz` control selected for the first 50-step static validation sweep.
- [2026-03-15] Selected `TP=2, PP=1, DP=8` as the next feasible topology after `TP=2, PP=2, DP=4`: it stays on one 16×V100 node, removes pipeline-parallel bubble entirely, remains proxy-distinct from both `TP=4, PP=1, DP=4` and `TP=2, PP=2, DP=4`, and passed remote `VALIDATE_ONLY=1` on `sd@v100x16-1`.
- [2026-03-15] Completed the detached `TP=2, PP=2, DP=4` 50-step transfer validation on `sd@v100x16-1` for `1072/1080/1087/1125 MHz`. The transferred default `1080 MHz` matched the measured minimum-energy point exactly, while `1125 MHz` was the fastest tested point; absolute runtime remained pessimistic (`runtime_ratio_mape≈7.76%`, `energy_ratio_mape≈5.01%`), but sweet-spot ranking generalized much better than on the earlier `TP=2, PP=4, DP=2` transfer.
- [2026-03-15] Chose `TP=2, PP=2, DP=4` as the next feasible, proxy-distinct 16-GPU topology for generalization testing on `sd@v100x16-1`; remote `VALIDATE_ONLY=1` passed, the 50-step unfixed baseline completed at `1030.6 s / 3686.1 W / 3798903.8 J`, and the transferred predictor (reusing the corrected `TP=2, PP=4, DP=2` calibration) currently recommends the `1072/1080/1087 MHz` neighborhood with default `1080 MHz`.
- [2026-03-15] Launched detached remote 50-step static validation for `TP=2, PP=2, DP=4` in session `power_tp2pp2dp4_pred50_20260315_161856`, covering `1072/1080/1087 MHz` plus a `1125 MHz` control.
- [2026-03-15] The user confirmed the clock-lock cleanup issue is solved, so cleanup/reset paths should stay quiet on success and only emit warnings on failures; validation effort now shifts to testing predictor transfer on other feasible topologies.
- [2026-03-15] Hardened experiment cleanup for communication-frequency runs on V100 hosts: `scripts/run_experiment.sh` now resets visible GPU clocks on shell `EXIT` for `dynamic` and `dryrun` too (not just `static`), and `megatron/gpu_freq_manager.py` now registers an `atexit` shutdown hook so lingering high-clock idle GPUs are less likely after training exits.
- [2026-03-15] Implemented a two-band residual correction layer over the hardware-first predictor: low-frequency and smooth mid/high-frequency corrections are now calibrated directly against sampled-point `total_time` and `total_energy`. On the full `TP=2, PP=4, DP=2` merged sweep, this improved sampled-point `total_time_mape` from `~1.73%` to `~1.20%` and `total_energy_mape` from `~1.20%` to `~0.90%`, while moving the continuous default recommendation from `1020 MHz` up to `1125 MHz`.
- [2026-03-15] Re-ran the `TP=2, PP=4, DP=2` Pareto-first prediction with the updated PP-bubble proxy against the synced pattaya artifacts and wrote fresh outputs to `.context/real-v100-tp2pp4dp2-rerun-20260315/`. The new analytic fit slightly improved `total_time` / runtime-ratio error (`MAPE≈1.73%` vs `1.77%`) but worsened `total_energy` / energy-ratio error (`MAPE≈1.20%` vs `1.05%`), and the continuous default recommendation still drifted low (`1020 MHz`), so pointwise fit remains strong while global Pareto shape is still not tight enough for direct recommendation.
- [2026-03-15] Tightened the PP-bubble analytic proxy: `analysis/freq_model/features.py` now uses pipeline bubble exposure to inflate the `PP` communication term in addition to the existing `pipeline_parallel_efficiency`, so `TP=2, PP=4, DP=2` no longer collapses to the old `TP=4, PP=1, DP=4` communication share and low-microbatch PP runs are penalized more realistically.
- [2026-03-15] Completed a guard-vs-formula audit of the offline predictor: workload-consistency matching now also checks `num_attention_heads`, `num_key_value_heads`, and `swiglu`, and the CLI now rejects a mismatched `baseline_root` workload before calibration/reporting.
- [2026-03-15] Fixed the predictor's workload-consistency gate so `micro_batch_size` is now part of `scripts/predict_freq_sweet_spot.py` signature matching; mixed-microbatch static runs will now fail fast instead of being merged into one prediction bundle even though PP bubble efficiency depends on them.
- [2026-03-15] Audited the offline frequency predictor's workload handling: `seq_length`, `global_batch_size`, and `micro_batch_size` are already wired into `analysis/freq_model` feature derivation and fixed-workload total-time/total-energy estimates, but `scripts/predict_freq_sweet_spot.py` still omits `micro_batch_size` from its workload-consistency signature, so mixed-microbatch runs could be merged accidentally even though PP bubble efficiency depends on them.
- [2026-03-13] Re-ran the Pareto-first accuracy report on the merged `TP=2, PP=4, DP=2` full 11-point sweep (`900/915/930/1185/1192/1200/1207/1215/1245/1252/1260 MHz`). Analytic prediction accuracy on sampled points is now quantified at about `1.77%` MAPE for `runtime_ratio_vs_baseline` and `1.05%` MAPE for `energy_ratio_vs_baseline`, but the continuous default recommendation still drifts too low (`1027 MHz`) because the curve remains slightly too optimistic about low-frequency energy savings and too pessimistic about high-frequency energy cost.
- [2026-03-13] Reframed `scripts/predict_freq_sweet_spot.py` outputs around Pareto-first review: the report now leads with the full predicted Pareto frontier and adds an explicit observed-vs-predicted accuracy section for `runtime_ratio_vs_baseline` and `energy_ratio_vs_baseline`, computed from analytic predictions without observed-overlay interpolation.
- [2026-03-13] Updated the prediction-layer objective so the default `supported_sweet_spot` now selects from the Pareto frontier by `total_energy` first and `total_time` second, while preserving the old equal-weight runtime/energy choice as `supported_balanced_sweet_spot` for comparison.
- [2026-03-13] Updated the expanded transfer comparison artifacts at `.context/real-v100-tp2pp4dp2-static-20260312/measured_vs_predicted_v2_pp_bubble_full_with_refine.tsv` and `final_analysis_v2_pp_bubble_with_refine.md`; the bubble-aware model still overshoots upward (`~1252 MHz` predicted sweet spot), but it now clearly points into the correct high-efficiency band rather than the original failed `900-930 MHz` neighborhood.
- [2026-03-13] The remote refinement sweep `power_tp2pp4dp2_static_refine_20260313_1238c` completed successfully for `1185 / 1192 / 1207 / 1215 MHz`; the measured winner is now `1185 MHz` with Zeus summary `449.706 s`, `1089279.7 J`, `2422.2 W`, `0.601645 tokens/J`, beating the previous `1200 MHz` point on energy efficiency while staying near the same runtime band.
- [2026-03-13] Restored context from the memory bank, synced the completed `1245/1252/1260 MHz` remote artifacts for `TP=2, PP=4, DP=2`, and confirmed they do **not** beat the existing `1200 MHz` point on measured `tokens/J`; the bubble-aware predictor identified the right higher-frequency neighborhood directionally, but still overshot the measured discrete winner.
- [2026-03-13] Launched a narrower remote follow-up sweep around the measured `1200 MHz` region in detached `screen` session `power_tp2pp4dp2_static_refine_20260313_1238c`, covering `1185 / 1192 / 1207 / 1215 MHz` with `DISABLE_CHECKPOINT=1` restored.
- [2026-03-13] Found that the remote repo still contains a stale top-level `run_experiment.sh` without `DISABLE_CHECKPOINT` support; energy-only remote launches must call `scripts/run_experiment.sh` explicitly.
- [2026-03-12] Attempted the originally preferred `TP=8, DP=2` topology on Qwen2.5-7B, but the real run failed immediately because the model shape is incompatible with `TP=8`: `num_attention_heads=28` is not divisible by `8`, and grouped-query `num_key_value_heads=4` would also be incompatible with `TP=8`.
- [2026-03-12] Checked several 16-GPU fallback decompositions against the current stable topology proxy and found that `TP=4, DP=4`, `TP=8, DP=2`, and `TP=4, PP=2, DP=2` all collapse to the same communication feature under the existing analytic model, so they are poor choices for a meaningful topology-transfer test without further modeling changes.
- [2026-03-12] Identified `TP=2, PP=4, DP=2` as the nearest feasible and currently model-visible `DP=2` fallback topology for Qwen2.5-7B on 16 V100s; `VALIDATE_ONLY=1` passed and a detached unfixed-baseline run was launched in remote `screen` session `power_tp2pp4dp2_baseline_20260312_1944`.
- [2026-03-12] The fallback `TP=2, PP=4, DP=2` unfixed baseline completed cleanly with Zeus summary `419.508 s`, `1515838.132 J`, `3613.370 W`, `0.432342 tokens/J`, which is materially slower but lower-power than the old `TP=4, PP=1, DP=4` baseline.
- [2026-03-12] Reused the old-topology hardware-first calibration (`predictions-hw-first-v3`) for a first zero-shot transfer prediction on the new `TP=2, PP=4, DP=2` baseline; the baseline-relative supported sweet spot came out around `915 MHz`, with neighborhood recommendation `[900, 907, 915, 922, 930]`.
- [2026-03-12] Started a detached validation sweep for the transferred recommendation on `TP=2, PP=4, DP=2` covering `900/915/930 MHz` plus a `1200 MHz` control in remote `screen` session `power_tp2pp4dp2_static_validate_20260312_2000`.
- [2026-03-12] Synced the first three completed static validation points (`900/915/930 MHz`) for `TP=2, PP=4, DP=2` and wrote `.context/real-v100-tp2pp4dp2-static-20260312/measured_vs_predicted_partial.tsv`; all three beat the new baseline on total energy, but the zero-shot transfer model materially overpredicted throughput and underestimated runtime ratio (predicted `~0.93-0.96x`, measured `~1.36-1.40x`).
- [2026-03-12] Completed the `1200 MHz` control for `TP=2, PP=4, DP=2` and finalized `.context/real-v100-tp2pp4dp2-static-20260312/measured_vs_predicted.tsv`: among the tested points, `1200 MHz` is best (`tokens/J=0.596836`, runtime ratio `1.0605`, energy ratio `0.7244`), outperforming the zero-shot recommended `900-930 MHz` band.
- [2026-03-12] The first topology-transfer verdict is now clear: the current hardware-first zero-shot model keeps the energy-saving direction but does not retain good sweet-spot ranking after shifting to `TP=2, PP=4, DP=2`; it overestimates low-frequency throughput and likely misses PP/bubble-style topology overhead even on this NvLink-equipped machine.
- [2026-03-12] Adjusted the predictor so `PP` communication cost is no longer discounted relative to `TP` in the workload proxy, and added an explicit pipeline-parallel efficiency term based on microbatches-per-step vs pipeline depth. With this bubble-aware revision, the old topology still predicts `~1252 MHz` and the `TP=2, PP=4, DP=2` zero-shot sweet spot moves back to `~1252 MHz`, much closer to the measured `1200 MHz` control winner.
- [2026-03-12] Refactored the offline frequency predictor toward a baseline-relative, hardware-first objective by calibrating against runtime/energy ratios versus the unfixed baseline, making observed-overlay use opt-in rather than default.
- [2026-03-12] Added a throughput-saturation prior to `analysis/freq_model/model.py` and centered calibration candidates on observed throughput scale rather than raw theoretical token-rate anchors, which prevents the pure analytic model from collapsing to unrealistic low clocks or over-trusting max-clock throughput.
- [2026-03-12] Smoke-validated the refactored predictor manually (without pytest, because local test bootstrap still requires `deepspeed`) and re-ran the real merged V100 CLI locally; the hardware-first prediction now recommends about `1245/1252/1260 MHz` with balanced sweet spot `1252 MHz` against the fresh unfixed baseline.
- [2026-03-12] Added focused freq-model regression coverage for three recent risk points: exact `PP` bubble-efficiency math, throughput-saturation early-clamp behavior, and `load_experiment_samples()` fallback parsing from Zeus log lines when `events.jsonl` is missing.
- [2026-03-12] Refactored the offline frequency predictor toward a baseline-relative, hardware-first objective by calibrating against runtime/energy ratios versus the unfixed baseline, making observed-overlay use opt-in rather than default.
- [2026-03-12] Added a throughput-saturation prior to `analysis/freq_model/model.py` and centered calibration candidates on observed throughput scale rather than raw theoretical token-rate anchors, which prevents the pure analytic model from collapsing to unrealistically low clocks or over-trusting max-clock throughput.
- [2026-03-12] Smoke-validated the refactored predictor manually (without pytest, because local test bootstrap still requires `deepspeed`) and re-ran the real merged V100 CLI locally; the hardware-first prediction now recommends about `1245/1252/1260 MHz` with balanced sweet spot `1252 MHz` against the fresh unfixed baseline.
- [2026-03-11] Added short-run-friendly warmup and eval controls in `scripts/run_experiment.sh`.
- [2026-03-11] Updated NVML lock/reset helpers in `scripts/experiment_utils.sh` to use elevated remote execution.
- [2026-03-11] Extended Zeus integration in `megatron/power_monitor.py` and `megatron/training.py` to preserve `samples/Wh` and `tokens/J`, including leftover/final interval capture.
- [2026-03-11] Added `DISABLE_CHECKPOINT=1` support to `scripts/run_experiment.sh` and validated it remotely.
- [2026-03-11] Switched remote experiment launches to isolated `screen` sessions and recorded session IDs in `.context/remote-screen-sessions.tsv`.
- [2026-03-11] Defined a theory-led, calibration-light frequency sweet-spot prediction workflow and documented it in `docs/plans/2026-03-11-frequency-model-design.md`.
- [2026-03-12] Implemented the first offline prediction scaffold in `analysis/freq_model/` plus `scripts/predict_freq_sweet_spot.py`, with targeted unit coverage.
- [2026-03-12] Smoke-validated the prediction CLI on synthetic experiment artifacts in `.context/freq-model-smoke/`; it produced a discrete recommendation set `[1110, 1200, 1305]` but showed large throughput error on synthetic data.
- [2026-03-12] Ran the first real V100 prediction on `.context/real-v100-power-static-20260312` using five static sweep points (`1200/1305/1380/1410/1597 MHz`). The model used real V100 NVML frequency tables and extrapolated a lower-frequency optimum near `1025 MHz`, recommending `[1020, 1027, 1035]`.
- [2026-03-12] Executed a serial real-V100 validation sweep at `1020/1027/1035 MHz` in remote `screen` session `power_lowfreq_20260312_113029b`. All three runs completed successfully and each beat the earlier `1200 MHz` point on `tokens/J`.
- [2026-03-12] Updated `analysis/freq_model/recommend.py` and `scripts/predict_freq_sweet_spot.py` so recommendations now maximize efficiency only within a configurable runtime-slowdown guardrail, and report estimated total time/energy for a fixed comparison workload.
- [2026-03-12] Synced the real low-frequency validation artifacts (`1020/1027/1035 MHz`) into `.context/real-v100-power-static-20260312-lowfreq/`, built an 8-point merged sweep under `.context/real-v100-power-static-20260312-merged/`, and compared measured-vs-modeled guardrail choices.
- [2026-03-12] Added an observed-frequency interpolation overlay in `analysis/freq_model/recommend.py` and now trust the observed frequency range once enough real points exist; on the merged 8-point V100 sweep this brings guardrail recommendations back in line with measured sweet spots (`1305 MHz` at `1.00x`, around `1200 MHz` near `1.10x`, and `1035/1020 MHz` by `1.25x/1.30x`).
- [2026-03-12] Reframed the analysis around baseline-relative tradeoffs: the usable unfixed baseline is `power_baseline_20260311_20b`, and the current Pareto frontier versus that baseline is `1305/1200/1035/1020 MHz` from faster-to-greener.
- [2026-03-12] Launched a fresh unfixed-baseline rerun in remote `screen` session `power_baseline_rerun_20260312_1512`, with run ID `v100_power_baseline_rerun_20260312_151032_DGX2-1` and `DISABLE_CHECKPOINT=1` for clean energy/time comparison.
- [2026-03-12] The fresh baseline rerun completed cleanly with Zeus summary: `309.165 s`, `1387540.447 J`, `4488.029 W`, `0.472318 tokens/J`. Relative to this rerun, the Pareto frontier remains `1305/1200/1035/1020 MHz` from faster-to-greener.
- [2026-03-12] Added `scripts/plot_baseline_pareto.py` and generated both paper-style and analysis-style 2D Pareto skyline charts from the refreshed baseline-relative TSV.
- [2026-03-12] Modeling direction updated: keep the second-layer predictor hardware-first across the full frequency band, treat frequency itself as part of hardware capability, and avoid overfitting the current small sample set; use samples mainly to calibrate an analytic prior rather than to dominate it.

## Active Decisions
- [2026-03-12] 当前无需把连续曲线的人为低误差区域偏向已观测 Pareto 频点；像 `~1250 MHz` 这样的中间位置应只被视为曲线形状信息，而不是主要追求目标。
- [2026-03-12] 下一阶段优先改变硬件拓扑来检验模型泛化能力，首选对比场景是 `TP=8, DP=2`（保持其余条件尽量可比），观察 baseline-relative 预测与实测 Pareto/甜点是否仍保持较好一致性。
- [2026-03-12] Because `TP=8, DP=2` is structurally invalid for the current Qwen2.5-7B head layout, the working fallback for the first transferable `DP=2` validation is `TP=2, PP=4, DP=2` on the same 16-GPU node.
- [2026-03-12] The user clarified that this DGX/V100 machine has `NvLink`, so topology-transfer analysis should treat inter-GPU communication as stronger than plain PCIe and explicitly watch for the current model overestimating communication penalties on multi-stage topologies.
- [2026-03-12] For the current transfer miss, prioritize modeling pipeline/topology overhead (for example `PP=4` bubble or stage-schedule effects) before further tweaking raw communication-bandwidth assumptions.
- [2026-03-12] For now, treat `PP` and `TP` communication pressure as same-order effects in the topology proxy, then use a separate pipeline bubble efficiency term to account for deeper `PP` throughput loss without forcing an artificial preference for one isolated frequency point.
- [2026-03-11] V100 work is focused on static frequency energy efficiency, not quantization.
- [2026-03-11] Real runs should happen on `sd@v100x16-1` first.
- [2026-03-11] Energy-only sweeps should avoid checkpoint writes unless explicitly requested.
- [2026-03-11] The new prediction layer will model `throughput(f)` and `power(f)` first, then derive efficiency metrics such as `tokens/J`.
- [2026-03-11] The prediction layer should prioritize hardware/task features and use only a small number of interpretable calibration parameters.
- [2026-03-12] Real V100 prediction runs should start from the comparable `power_static*` sweep artifacts before mixing in baseline or short-run variants.
- [2026-03-12] The current curve analysis should use the unfixed baseline (default GPU frequency behavior) as the comparison anchor and report each static frequency by its total-energy ratio and total-runtime ratio versus baseline.
- [2026-03-12] Do not impose a hard runtime-acceptance guardrail for now; instead present the baseline-relative energy/time tradeoff so different trainers can choose according to whether they care more about energy or elapsed time.
- [2026-03-12] Because the end-to-end training regime is expected to stay stable, per-step power and step-time measurements are acceptable proxy observables for total-energy and total-runtime behavior, as long as comparisons keep workload and setup fixed.

## Next Steps
- Switch the next generalization target to `TP=2, PP=8, DP=1` on `sd@v100x16-1`: it stays feasible under the current Qwen2.5-7B head partition constraints, keeps `TP=2` so we do not re-entangle the already-debugged `TP=1` anchoring issue, and doubles pipeline depth while removing DP overlap, making it a strong next test of full-curve `total_time` / power transfer accuracy.
- Keep using `python -m pytest --noconftest tests/unit_tests/test_freq_model.py` for local freq-model regression checks until the repo-wide pytest bootstrap can import `deepspeed` again.
- For the invalid `TP=8, DP=2` request, preserve the failure as a hard feasibility note (`28` attention heads / `4` KV heads cannot be partitioned by `TP=8`).
- Wait for the `TP=2, PP=4, DP=2` static validation sweep (`900/915/930/1200 MHz`) to finish, sync the artifacts locally, and compare measured runtime/energy ratios against the zero-shot transfer prediction.
- If the `1200 MHz` control confirms the same pattern, update the topology-transfer conclusion to emphasize missing PP/topology overhead (not just raw communication bandwidth) in the current hardware-first analytic prior.
- Rework the transfer-oriented analytic prior so topology changes with higher pipeline depth do not inherit overly optimistic low-frequency throughput from the old topology calibration.
- For the next validation round on `TP=2, PP=4, DP=2`, center the follow-up frequency search around the newly observed `1200 MHz` region rather than the failed zero-shot `900-930 MHz` neighborhood.
- If the continuous curve looks best near an unsampled region such as `~1250 MHz`, treat that only as evidence about curve shape; do not elevate it into a primary objective or bias the model toward matching a single preferred point.
- Build an offline artifact loader that extracts workload, topology, and Zeus metrics from `run.json`, `events.jsonl`, and logs.
- Refit and reassess the prediction layer now that the local merged 8-point dataset (`1020/1027/1035/1200/1305/1380/1410/1597 MHz`) is available for calibration.
- Tighten the interpolation/overlay policy and calibration reporting using the real 8-point V100 sweep, especially around the `1.02x-1.20x` guardrail region where the model now tracks measured choices closely but still prefers interpolated in-between clocks rather than exact sampled points.
- Refit the new runtime-guarded recommendation layer using the expanded real V100 sweep so the guardrail and energy ranking stay well calibrated below `1200 MHz`.
- Continue the V100 short-run sweep with remaining candidate frequencies such as `1500 MHz`.
- For each topology, keep reporting the continuous curve plus the measured/supported Pareto options, rather than collapsing the analysis into a single preferred static clock.
- Optionally improve tracker finalize behavior when runs exit without checkpointing or when finalization is partially incomplete.

## Important Patterns & Preferences
- Use remote `screen` for runs that take more than a quick validation.
- Record `screen` session IDs and attachment commands immediately after launch.
- Preserve both `samples/Wh` and `tokens/J` in experiment summaries.
- Keep experiment conditions identical except for the frequency policy being tested.
- When comparing candidate frequencies, report baseline-relative total energy and total elapsed time first; avoid collapsing the tradeoff into a single hard constraint before the user decides the preferred balance.

## Current Blockers
- `run.json` finalization is still less reliable than log/events parsing for some runs.
- Sweep coverage is still incomplete without a `1500 MHz` data point.

- [2026-03-15] Remote 50-step validation on `sd@v100x16-1` now confirms the corrected curve is close on absolute runtime but still slightly overestimates total power/energy around the new recommended band: `1117 MHz` finished at `1187.3 s / 2231.8 W / 2649800.5 J` versus predicted `1200.6 s / 2280.9 W / 2738488.1 J`, and `1125 MHz` finished at `1178.7 s / 2258.5 W / 2662089.4 J` versus predicted `1193.0 s / 2295.3 W / 2738376.8 J`.
- [2026-03-15] The current corrected curve gets the local runtime slope between `1117 -> 1125 MHz` mostly right (`-7.6 s` predicted vs `-8.6 s` observed over 50 steps), but its energy slope is still too flat / slightly wrong-signed (`-111 J` predicted vs `+12289 J` observed), so the next calibration pass should steepen the mid/high-band energy rise without breaking the good runtime fit.

- [2026-03-15] The third 50-step validation point also completed: `1132 MHz` finished at `1172.9 s / 2280.5 W / 2674848.9 J` versus predicted `1186.5 s / 2308.0 W / 2738406.6 J`. Across `1117/1125/1132 MHz`, the corrected curve now stays within about `1.17%` runtime MAPE, `1.68%` power MAPE, and `2.91%` total-energy MAPE on real 50-step runs, but it still underestimates how quickly energy rises through the mid/high-frequency band.
